```markdown
I started a little personal project between friends a while ago, an NPM package that provides easier access to several imageboard APIs (boorus) in consistent object formats
```

```markdown
I initially named it _"Booru Abuse"_ because I intended on fetching data from the frontend of certain sites whose API _really_ sucks--my chat history in [the JavaScript room](https://chat.stackoverflow.com/rooms/17/javascript) should explain (and I can send some excerpts if asked)--but as I spent more time on it, I learned that using the frontend HTML of the site not only isn't reliable but isn't allowed according to staff
```

```markdown
It's only still called _Booru Abuse_ because I thought it still had that ring to it (and definitely not because I've renamed this project like 2 or 3 times already), and when another friend saw me working on it from my Discord presence, they assumed I was working on a _booru_ for _abuse content_
```

```markdown
I'm pretty certain on "booru.js", but I want to be certain ...
```
